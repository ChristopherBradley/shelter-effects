name: veg_species_mapper
channels:
  - conda-forge
dependencies:
  - python=3.12
  # core POC: imagery download + perspective crops + species ID + triangulation
  - requests>=2.31
  - numpy>=1.26
  - pillow>=10.0
  # Sentinel-2 fetch (geospatial -- conda-forge handles GDAL/PROJ cleanly)
  - rasterio>=1.3
  - pystac-client>=0.7
  - pip
  - pip:
      - planetary-computer>=1.0
